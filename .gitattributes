# Стабильные окончания строк: без этого CRLF (Windows) vs LF (Linux)
# каждый раз ломают стабильность workbox-ревизий между пересборками.
* text=auto eol=lf

# Бинарные — не трогаем
*.png binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
